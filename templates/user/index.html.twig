{% extends 'base.html.twig' %}

{% block title %}User index
{% endblock %}

{% block body %}

	<div class="container-fluid">
		<h1 class="text-ticket-pending">Utilisateurs</h1>
		<div class="card bg-white main-section">
			<div class="card-body">
				<div class="d-flex flex-wrap justify-content-center">
					{%  for user in users %}
							<div class="me-3 mb-3">
								{% include 'components/user_card.html.twig' with {'user': user}   %}
							</div>
					{% endfor %}
				</div>
			</div>
		</div>
	</div>
	{# <table class="table">
		<thead>
			<tr>
				<th>Id</th>
				<th>Email</th>
				<th>Roles</th>
				<th>Password</th>
				<th>FirstName</th>
				<th>LastName</th>
				<th>Pseudo</th>
				<th>actions</th>
			</tr>
		</thead>
		<tbody>
			{% for user in users %}
				<tr>
					<td>{{ user.id }}</td>
					<td>{{ user.email }}</td>
					<td>{{ user.roles ? user.roles|json_encode : '' }}</td>
					<td>{{ user.password }}</td>
					<td>{{ user.firstName }}</td>
					<td>{{ user.lastName }}</td>
					<td>{{ user.pseudo }}</td>
					<td>
						<a href="{{ path('app_user_show', {'id': user.id}) }}">show</a>
						<a href="{{ path('app_user_edit', {'id': user.id}) }}">edit</a>
					</td>
				</tr>
			{% else %}
				<tr>
					<td colspan="8">no records found</td>
				</tr>
			{% endfor %}
		</tbody>
	</table> #}

	<a href="{{ path('app_user_new') }}">Create new</a>
{% endblock %}
